# Incidents

Written record of things that broke, what caught them, and what changed.

One file per incident, each covering: what broke, how it was detected, what the
impact was, what changed as a result, and which eval case now covers it.

Incidents where the failure was caught before deploy are as valuable to record
as ones that reached production — arguably more so, since they document the
detection working.

*Empty.*
